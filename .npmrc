# pnpm 10 already links `workspace:*` dependencies, auto-installs peers and
# tolerates peer mismatches; the previous explicit copies of those defaults were
# only noise. Engine mismatches must fail the install (see engines in
# package.json), so the Node floor cannot silently drift below what Vite needs.
engine-strict=true
