import type { ServicePageRoute } from '@localdeck/shared'; import type { UiServiceModule } from
'../types'; import { CreatePage } from './pages/Create'; import { DetailPage } from
'./pages/Detail'; import { ListPage } from './pages/List'; import { spec } from './spec'; /** *
{{displayName}}
console module — generated by `pnpm turbo gen service`. * * The shell discovers every service module
by globbing service folders, so * default-exporting a module here is the only wiring the sidebar,
the global * search and the router need. */ const routes: readonly ServicePageRoute[] = [ { path:
'', title: '{{displayName}}', page: 'list' }, { path: 'create', title: 'Create', page: 'create' }, {
path: ':resourceId', title: 'Details', page: 'detail' }, ]; export const serviceModule:
UiServiceModule = { descriptor: spec.descriptor, spec, routes, pages: { list: ListPage, detail:
DetailPage, create: CreatePage }, }; export default serviceModule;